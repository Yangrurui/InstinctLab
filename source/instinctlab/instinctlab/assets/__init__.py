"""Robot assets.

``unitree_g1_spec`` holds the engine-neutral catalog and ``RobotSpec``; ``unitree_g1`` holds the
Isaac Lab configs built from it. Import the module you need directly -- there is no indirection here
to route a lookup through.
"""
