# InstinctLab

Task application, G1 assets, and train/play configuration for the engine-neutral
InstinctLab contract. Install it with `instinctlab-engine-core` and one or both
backend distributions. The repository-level README contains setup, dataset,
training, and playback instructions.
