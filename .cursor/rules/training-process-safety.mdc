---
description: 长时间训练进程保护——禁止 agent 在用户未明确要求时停止、重启或 pkill 训练。
alwaysApply: true
---

# 训练进程保护

用户没有明确说停/关/重启训练时，**不要动正在跑的 `scripts/train.py`（及等价入口）**。

- 禁止为换 GPU、改日志、提交代码等理由擅自 `pkill` / 重启
- 允许读日志、对拍指标、启动 play（注意 GPU 占用）
- 启动新训练前：`pgrep -af "scripts/train.py"` + `nvidia-smi`；目标 GPU 已被占用则**先问用户**，不要默认杀进程
- 必须重启时用新日志名或新 `run_name`，不要覆盖仍在用的日志

与 [`multi-engine-training.mdc`](multi-engine-training.mdc) 里 `train.py` 入口约束配套。
